# Backend Local Development

From `OCMS-Backend`:

```bash
npm run dev
npm run lint
npm run build
```

## Notes

- The API runs on port `5000` by default.
- CORS and API origin settings live in `src/app.js`.
- Do not hardcode frontend assumptions in controllers.
